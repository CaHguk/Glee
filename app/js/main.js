$(function () {
  $('.hero__product-slider').slick({
    dots: true,
    arrows: false,
    autoplay: false,
    autoplaySpeed: 2000
  });

  var mixer = mixitup('.popular__list');

  var mixer = mixitup('.design__list');
});